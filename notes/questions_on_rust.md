# Rust & Bevy
- Is Projection::Orthographic() a type? Why can't we just let ref mut orthographic = *camera.into_inner?
- Can a mutable reference be given to multiple functions at the same time, or can only one mutable reference be borrowed at a time?